global:
  resolve_timeout: 5m

# route:
#   group_by: ['alertname']
#   group_wait: 30s
#   group_interval: 5m
#   repeat_interval: 1h
#   receiver: 'telegram'

# receivers:
#   - name: 'telegram'
#     telegram_configs:
#       - api_url: 'https://api.telegram.org'
#         bot_token: ${TELEGRAM_BOT_TOKEN}
#         chat_id: ${TELEGRAM_CHAT_ID}

route:
  receiver: 'versus-incident'
  group_wait: 10s

receivers:
- name: 'versus-incident'
  webhook_configs:
  - url: 'http://versus-host:3001/api/incidents' # Versus API endpoint
    send_resolved: false
